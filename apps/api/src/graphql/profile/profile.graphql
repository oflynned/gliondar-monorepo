type Profile {
  id: ID!
  name: String!
  initials: String!
  fluency: Fluency!
  preferredLanguage: PreferredLanguage!
  user: User
}

enum Fluency {
  NATIVE,
  ADVANCED,
  INTERMEDIATE,
  BEGINNER
}
