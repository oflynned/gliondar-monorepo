scalar DateTime

type Query {
    getTime: DateTime!
}

type Mutation {
    setTime: DateTime!
}

enum PreferredLanguage {
    ENGLISH
    IRISH
}

enum ContentLanguage {
    IRISH
    ENGLISH
    UNKNOWN
}
