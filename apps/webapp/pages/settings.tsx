import { NavBarPage, SideBarLayout } from '../design-system';

const Settings = () => {
  return (
    <SideBarLayout activePage={NavBarPage.SETTINGS}>Settings</SideBarLayout>
  );
};

export default Settings;
